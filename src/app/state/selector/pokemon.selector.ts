import { pokemonFeature } from "../feature/pokemon.feature";

export const {
  selectEntities,
  selectIds,
  selectData,
  selectPokemonsConsulted,
  selectPokemonConsulted,
  selectPokemonFeatureState,
  selectPokemonListInit
} = pokemonFeature
